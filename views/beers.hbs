
{{#each beersFromApi}}
<div class="container">
    <div class="cards">
        <h3>{{this.name}}</h3>
        <img src="{{this.image_url}}" alt="{{this.name}}">
        <p>{{this.description}}</p>
    </div>
</div>
{{/each}}


{{!-- {{#each beersFromApi}}
{{> beerpartials.hbs}}
{{/each}} --}}